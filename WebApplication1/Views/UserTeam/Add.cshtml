@model HRM.DAL.Models.UserTeam
@{
    ViewBag.Title = "Add";
}

<div class="container" align="center">
    <h4>Add UserTeam</h4>
    <div class="panel-footer " style="width: 250px; border:1px solid black;color:cornflowerblue" >
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-horizontal">  


                <div class="form-group">
                    @Html.LabelFor(i => i.UserId)
                    <div class="col-md-10">
                        @Html.EditorFor(i => i.UserId)
                    </div>
                </div>
                <div class="form-group">
                    @Html.LabelFor(i => i.TeamId)
                    <div class="col-md-10">
                        @Html.EditorFor(i => i.TeamId)
                    </div>
                </div>

                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <input type="submit" value="Add" class="btn btn-primary" />
                    </div>
                </div>

            </div>

        }
        <div>
            @Html.ActionLink("Back to List", "Index")
        </div>
    </div>

</div>

